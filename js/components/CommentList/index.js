import Vue from '../../dist/vue.common.js';
import template from './template.html';
import compileTemplate from '../../utils/compileTemplate';

const CommentList = Vue.extend({
  template: template
});

export default CommentList;
